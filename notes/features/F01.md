# F01 - Parity + auth contract spec (read-only discovery)

## Acceptance criteria
- docs/PARITY.md exists and includes:
  - Reader read/search routes and patterns to match
  - MCP routes and tool schemas to match
  - auth error semantics (missing key / invalid key / quota)
- docs/AUTH_CONTRACT.md exists and includes:
  - exact HTTP endpoints Reader/MCP call for auth (paths, headers, JSON)
  - required response fields
  - expected status codes per scenario
- No code changes yet (unless needed to compile docs from discovery).
