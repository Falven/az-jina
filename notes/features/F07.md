# F07 - Custom domains + cert + DNS handoff

## Acceptance criteria
- docs/DOMAIN_SETUP.md includes exact Squarespace DNS records (CNAME/TXT) for:
  - ${READER_SUBDOMAIN}.${BASE_DOMAIN}
  - ${SEARCH_SUBDOMAIN}.${BASE_DOMAIN} (if Jina uses distinct hostnames)
  - ${MCP_SUBDOMAIN}.${BASE_DOMAIN}
  - ${AUTH_SUBDOMAIN}.${BASE_DOMAIN} only if auth must be public (prefer internal)
- After DNS is configured by the human, a single toggle + apply completes bindings.
