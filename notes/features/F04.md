# F04 - Wire Reader + MCP to our auth service (minimal submodule changes)

## Acceptance criteria
- Reader denies requests without key and allows with valid key (same semantics as Jina).
- MCP denies/permits similarly.
- Reader/MCP call our auth service (prove via logs + request traces).
