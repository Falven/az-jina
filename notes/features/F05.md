# F05 - Infra/ops deployment on Azure Container Apps

## Acceptance criteria
- uv run python -m ops.deploy_bootstrap ${ENV}
- uv run python -m ops.deploy_workload_reader ${ENV}
- uv run python -m ops.deploy_workload ${ENV} (or whatever script maps to MCP/auth)
- Re-running is idempotent.
